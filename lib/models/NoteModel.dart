class Notemodel {
  final String title, description, dateTime;
  final int color;
  const Notemodel(
      {required this.dateTime,
      required this.description,
      required this.title,
      required this.color});
}
